# self-optimize

Recurring optimization loop for Claude Code. Private/personal tool; see SECURITY.md before running.

Install (from a checkout or the private repo):

    /plugin marketplace add /Users/nick/Documents/python/self-optimize
    /plugin install self-optimize@self-optimize

First run (zero LLM cost): `/self-optimize --stats-only`

Full docs land with v0.1.0 (see docs/evidence-schema.md).
