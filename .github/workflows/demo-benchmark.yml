name: demo benchmark
on:
  push:
    branches: [main]
  pull_request:
jobs:
  gym-on-demo-corpus:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: bash demo/run.sh
